import styled from 'styled-components';

export const Wrapper = styled.table`
    th {
        width: 10%;
        text-align: left;
    }

    td {
        width: 10%;
        text-align: left;
    }
`;