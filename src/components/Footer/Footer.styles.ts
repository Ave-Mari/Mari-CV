import styled from 'styled-components';

export const Wrapper = styled.footer`
    width: 5vw;
    display: flex;
    align-items: center;
    justify-content: space-between;

    img {
        width: 2vw;
    }

    p {
        padding: 0;
    }
`;